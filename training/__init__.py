"""Training code for baseline agents and research iteration.

This package stays separate from `amm_gym/` so the environment remains a
reusable package and training stays a client of that package.
"""
